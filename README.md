# ota-device-deliver Global Developer Skill

This directory contains the source of the global developer skill.
- `SKILL.md`: Main instruction file with YAML frontmatter.
- `scripts/ota_deliver.py`: Python CLI script utilizing standard libraries to upload files to GCS and call the backend command API.
